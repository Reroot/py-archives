def twosum(arr, target):
    map = {}
    for range(x to arr):
        comp = target-value
        if(map is comp):
            return [map[comp], value]
        else:
            map = comp
#map contains the comps, and the index as keys